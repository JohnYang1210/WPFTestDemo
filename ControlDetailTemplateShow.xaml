<UserControl x:Class="wpfTestStudio.ControlDetailTemplateShow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfTestStudio"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2.5*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="controlsListBox" Grid.Column="0" SelectionChanged="controlsListBox_SelectionChanged"/>
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Visible">
            <TextBlock x:Name="textTemplate" />
        </ScrollViewer>
        
        <Grid x:Name="grid">

        </Grid>
    </Grid>
    
</UserControl>
